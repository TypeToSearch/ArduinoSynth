BoxLayout:
    orientation: 'vertical'
    SampleSlider:
        id: samples
        num_sliders: 16
        size: (400, 100)
    Button:
        size_hint: (1, 0.25)
        text: 'Send'
        on_press: app.send()
    Label:
        text: app.status
        size_hint: (1, 0.1)
