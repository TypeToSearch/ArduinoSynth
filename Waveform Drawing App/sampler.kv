BoxLayout:
    orientation: 'vertical'
    SampleSlider:
        id: samples
        num_sliders: 128
        size: (400, 100)
    BoxLayout:
        orientation: 'horizontal'
        size_hint: (1, 0.25)
        Button:
            text: 'Connect'
        Button:
            text: 'Send'
            on_press: print(samples.get_scaled_values())
